import fs from 'node:fs';import path from 'node:path';import {GameRuntime} from '../v7/dist/engine.js';import {ageStage} from '../v7/dist/content.js';
class FsContent{constructor(root){this.root=root;this._base=null;this.cache=new Map()}async base(){return this._base??=JSON.parse(fs.readFileSync(path.join(this.root,'base.json'),'utf8'))}async events(age){const stage=ageStage(age);if(!this.cache.has(stage))this.cache.set(stage,JSON.parse(fs.readFileSync(path.join(this.root,`events-${stage}.json`),'utf8')));return this.cache.get(stage)}}
class FastPersistence{constructor(){this.snaps=new Map();this.meta=null}async save(){}async load(){return null}async putSnapshot(k,s){this.snaps.set(k,structuredClone(s))}async getSnapshot(k){const s=this.snaps.get(k);return s?structuredClone(s):null}async saveMeta(m){this.meta=structuredClone(m)}async loadMeta(){return this.meta?structuredClone(this.meta):null}}
const provider=new FsContent(path.join(process.cwd(),'v7/generated'));const base=await provider.base();if(base.CAREER_ROUTES.length<100)throw new Error(`V8.1 expected >=100 careers, got ${base.CAREER_ROUTES.length}`);if(base.TRAITS.length<400)throw new Error(`V8.1 trait pool too small ${base.TRAITS.length}`);if(!(base.RARITY.P.rank>base.RARITY.SSS.rank&&base.RARITY.P.weight<base.RARITY.SSS.weight))throw new Error('rarity hierarchy broken');
const q=(a,p)=>{const x=[...a].sort((a,b)=>a-b);return x[Math.floor((x.length-1)*p)]};const runs=Number(process.env.V7_RUNS||240),ages=[],wealths=[];let debt=0,partner=0,million=0,fate=0,noChoices=0,contacts=0,maxSteps=0,synergyRuns=0,routeRuns=0,evolutionRuns=0,zhouPrismatic=0,majorRelationshipEvents=0,ageSemanticViolations=0,sameChoiceTitle=0,choiceCategoryCollisions=0,choiceHands=0;const youngBad=/退休|养老现金流|公司重组|猎头|买房|房贷/;const oldBad=/幼儿园|家长会|同桌|升学|模拟考试|大学第一学期|实习面试/;
for(let i=0;i<runs;i++){const rt=new GameRuntime(provider,new FastPersistence());await rt.init(100000+i);const first=rt.store.get();if(first.wealth!==0)throw new Error(`infant wealth must start at zero, got ${first.wealth}`);if(first.currentChoices.length!==5||first.currentChoices.some(c=>c.rare)||first.currentChoices.some(c=>c.outcomes.length!==3))throw new Error('zhou ritual contract broken');if(first.traits.some(t=>!t.desc&&!t.effects&&!t.mods))throw new Error('trait hydration missing');let steps=0;const recentTitles=[];while(rt.store.get().alive&&rt.store.get().age<112&&steps++<125){const s=rt.store.get();if(!s.currentChoices.length){noChoices++;await rt.ensureChoices();if(!rt.store.get().currentChoices.length)throw new Error(`no choices at age ${s.age}`)}const titles=s.currentChoices.map(c=>c.title);if(new Set(titles).size!==titles.length)sameChoiceTitle++;const cats=s.currentChoices.map(c=>c.category);const specialHand=s.age===1||s.age===18||(s.age===22&&!s.careerRoute)||s.currentChoices.every(c=>c.sourceEvent==='lachesis');if(s.currentChoices.length>=4&&!specialHand){choiceHands++;const maxCat=Math.max(...[...new Set(cats)].map(c=>cats.filter(x=>x===c).length));if(maxCat>=3)choiceCategoryCollisions++;}for(const t of titles){if(s.age<=6&&youngBad.test(t))ageSemanticViolations++;if(s.age>=65&&oldBad.test(t))ageSemanticViolations++;if(recentTitles.slice(-8).includes(t)&&!t.includes('晋升'))sameChoiceTitle++;recentTitles.push(t)}const idx=(i+steps)%s.currentChoices.length;await rt.resolve(idx);const st=rt.store.get();if(st.ledger){const sum=st.ledger.items.reduce((a,b)=>a+b.amount,0);if(Math.abs(sum-st.ledger.net)>1)throw new Error(`ledger mismatch run ${i}`)}}const s=rt.store.get();maxSteps=Math.max(maxSteps,steps);ages.push(s.age);wealths.push(s.wealth);if(s.wealth<0)debt++;if(s.partnerId||s.relationships.some(r=>r.role==='伴侣'))partner++;if(s.wealthPeak>=1_000_000)million++;if(s.fate.triggered)fate++;contacts+=s.relationships.length;if(s.synergies.length)synergyRuns++;if(s.routesUnlocked.length)routeRuns++;if(s.traits.some(t=>(t.level??1)>1))evolutionRuns++;if(s.history.some(h=>h.sourceId==='zhou'&&h.rarity==='prismatic'))zhouPrismatic++;majorRelationshipEvents+=s.history.filter(h=>h.sourceId==='social_romance'||h.sourceId==='social_crisis').length;}
const categoryCollisionRate=choiceHands?choiceCategoryCollisions/choiceHands:0;const report={version:'V8.1',runs,medianAge:q(ages,.5),p90Age:q(ages,.9),age90plus:ages.filter(x=>x>=90).length/runs,debtRate:debt/runs,partnerRate:partner/runs,millionPeakRate:million/runs,fateRate:fate/runs,medianFinalWealth:q(wealths,.5),p10Wealth:q(wealths,.1),p90Wealth:q(wealths,.9),avgContacts:contacts/runs,synergyRate:synergyRuns/runs,routeRate:routeRuns/runs,evolutionRate:evolutionRuns/runs,zhouPrismaticRate:zhouPrismatic/runs,avgMajorRelationshipEvents:majorRelationshipEvents/runs,noChoices,maxSteps,ageSemanticViolations,sameChoiceTitle,choiceHands,choiceCategoryCollisions,categoryCollisionRate,generatedAt:new Date().toISOString()};
console.log('V8.1 player audit metrics',report);
if(report.medianAge<58||report.medianAge>94)throw new Error(`lifespan drift ${report.medianAge}`);if(report.age90plus>.48)throw new Error(`90+ too common ${report.age90plus}`);if(report.debtRate>.68)throw new Error(`debt rate too high ${report.debtRate}`);if(report.millionPeakRate>.90)throw new Error(`wealth too easy ${report.millionPeakRate}`);if(report.fateRate<.80)throw new Error(`fate trigger too low ${report.fateRate}`);if(report.synergyRate<.50)throw new Error(`build synergies too rare ${report.synergyRate}`);if(report.avgContacts>9)throw new Error(`relationship graph flooding ${report.avgContacts}`);if(report.avgMajorRelationshipEvents>5)throw new Error(`major person events too frequent ${report.avgMajorRelationshipEvents}`);if(report.noChoices>0)throw new Error(`choice deadlocks ${report.noChoices}`);if(report.ageSemanticViolations>0)throw new Error(`age-semantic violations ${report.ageSemanticViolations}`);if(report.categoryCollisionRate>.15)throw new Error(`choice categories too repetitive ${(report.categoryCollisionRate*100).toFixed(1)}%`);
{const p=new FastPersistence(),rt=new GameRuntime(provider,p);await rt.init(998877);if(rt.store.get().wealth!==0)throw new Error('fresh run infant wealth regression');await rt.resolve(0);if(!rt.store.get().history.length)throw new Error('zhou result missing from history');let foundResponses=false;for(let g=0;g<30&&rt.store.get().alive;g++){const st=rt.store.get();if(st.currentChoices.some(c=>c.responses?.length)){foundResponses=true;break}await rt.resolve(0)}if(!foundResponses)throw new Error('event responses never surfaced in 30 years');}
{const p=new FastPersistence(),rt=new GameRuntime(provider,p);await rt.init(998878);while(rt.store.get().age<rt.store.get().fate.targetAge)await rt.resolve(0);if(!rt.store.get().fate.snapshotKey)await rt.ensureChoices();const target=rt.store.get().age;await rt.resolve(0);while(rt.store.get().alive&&rt.store.get().age<Math.min(80,target+12))await rt.resolve(0);if(rt.store.get().alive)rt.store.dispatch({type:'DIE',reason:'回归测试'});const wl=rt.store.get().fate.worldline;if(!(await rt.rewindFate()))throw new Error('fate rewind failed');if(rt.store.get().age!==target||rt.store.get().fate.worldline!==wl+1||rt.store.get().currentChoices.length!==4)throw new Error('fate rewind state invalid');}
fs.writeFileSync(path.join(process.cwd(),'v7/generated/sim-report.json'),JSON.stringify(report,null,2));console.log('V8.1 simulation passed',report);
